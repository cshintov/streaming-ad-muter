{
  "version": {
    "release_notes": {
      "en-US": "Version 1.12 — audio ad detection now scoped to Zee5\n\nThe optional audio-based ad detector (beta, macOS) now runs only while you are watching a Zee5 stream — the case it was built for (live cricket with stitched-in ads). It no longer engages on Hotstar or SonyLIV, where ads are already caught precisely by the on-page and network detectors, and it no longer touches your system audio while you are on any other tab.\n\n- The companion helper connects only when a Zee5 stream is the active tab, and disconnects the moment you switch away.\n- No setup change. If you have the beta enabled, the popup now shows \"Active only on Zee5\" when you are elsewhere.\n\nFirst time installing the audio beta? It is macOS only and needs a small do-it-yourself helper: https://github.com/cshintov/streaming-ad-muter#audio-ad-detection-beta-macos-only\n\nThe existing ad muting for Hotstar, SonyLIV and Zee5 continues to work for everyone with no extra setup."
    },
    "approval_notes": "This extension includes an OPTIONAL, off-by-default audio ad-detection feature for live streams that server-side splice ads into the video (no DOM overlay, no distinguishable ad-pod network request), which the existing DOM/network detectors cannot catch. macOS only. In v1.12 this feature is scoped to Zee5 only: the native helper is connected only while the active tab is a zee5.com stream and is disconnected on any tab switch / navigation away (via tabs.onActivated / tabs.onUpdated / windows.onFocusChanged).\n\nNATIVE MESSAGING\n- The extension calls runtime.connectNative(\"com.devopsbytes.admute\") only when the user enables \"Audio ad detection (beta)\" in the popup (off by default) AND the active tab is a Zee5 stream.\n- The native host is a separate, user-installed helper. It is NOT and cannot be bundled in the XPI (native messaging hosts are OS-level executables, registered via a manifest under ~/Library/Application Support/Mozilla/NativeMessagingHosts/). There is no prebuilt or signed installer — it is a power-user/beta feature that users build from source themselves.\n- Full source of the helper is in the public repository under native/: https://github.com/cshintov/streaming-ad-muter/tree/master/native\n    - admute-detector: Python (standard library only) native-messaging host; runs the detection state machine and exchanges length-prefixed JSON over stdio.\n    - tapmon.swift: Core Audio process tap that captures system audio output (macOS 14.4+, no kernel extension or driver).\n    - install.sh: compiles tapmon (requires Xcode command line tools) and registers the native-messaging host manifest.\n\nPRIVACY / DATA\n- Audio is analysed entirely locally and is never stored or transmitted. The helper computes only two scalar features per analysis window (a frequency-band energy ratio and a loudness variance) to decide ad vs content.\n- The extension never receives any audio — only small JSON control messages such as {\"type\":\"ad\"} / {\"type\":\"content\"}.\n- Accordingly, data_collection_permissions is declared as \"none\".\n\nEXTENSION SOURCE\n- The extension ships hand-written, unminified JavaScript with no bundler, minifier, or transpiler step. The entire project (extension + native helper) is public at https://github.com/cshintov/streaming-ad-muter\n\nHappy to provide anything else needed."
  }
}
